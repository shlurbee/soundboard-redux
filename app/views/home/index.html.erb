
<img src=<%= @avatar_url %>>

<div id="public_tracks" class="tracks">
  You have <%= @public_tracks.size %> public tracks.
  <ul>
    <% @public_tracks.each do |t| %>
    <li>
      <%= t.title %>
    </li>
    <% end %>
  </ul>
</div>

<div id="private_tracks" class="tracks">
  You have <%= @private_tracks.size %> private tracks
  <ul>
    <% @private_tracks.each do |t| %>
    <li>
      <%= t.title %>
    </li>
    <% end %>
  </ul>

</div>

